box: phusion/baseimage
build:
  steps:
    - arjen/hugo-build:
        theme: redLounge
        flags: --buildDrafts=true
deploy:
  steps:
    - add-to-known_hosts:
        hostname: 188.226.136.184
        fingerprint: 2d:c1:bc:4c:1a:dd:96:db:af:00:7b:3c:50:df:2e:1c
    - mktemp:
        envvar: PRIVATEKEY_PATH
    - create-file:
        name: write key
        filename: $PRIVATEKEY_PATH
        content: $SSH_PRIVATE
        overwrite: true
    - script:
        name: transfer app
        code: |-
         pwd
         ls -la
         echo catting ssh_known_hosts
         cat /etc/ssh/ssh_known_hosts
         echo catting PRIVATEKEY_PATH
         echo $PRIVATEKEY_PATH
         cat $PRIVATEKEY_PATH
         scp -i $PRIVATEKEY_PATH -P $PORT -r public/* $USER@toonverbeek.com:/var/www/toonverbeek.com/blog/
          
